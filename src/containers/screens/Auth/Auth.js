import React, {Component} from "react";
import {ScrollView, Text} from "react-native";

class Auth extends Component {
    render() {
        return (
            <ScrollView>
                <Text>Something light</Text>
            </ScrollView>
        )
    }
}

export default Auth;